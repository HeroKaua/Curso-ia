document.addEventListener('DOMContentLoaded', function() {
    // Manipulador de evento para o formulário de envio do comprovante
    document.getElementById('payment-form').addEventListener('submit', function(event) {
        // Nenhuma validação é feita no JavaScript
        // O PHP se encarregará da validação do pagamento
    });
});
